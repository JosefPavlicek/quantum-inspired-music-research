"""Chord candidate generation.

V1 target:
I, ii, iii, IV, V, vi, vii°
plus common seventh-chord variants.
Secondary dominants are optional and disabled by default.
"""


def generate_candidates(key, config):
    raise NotImplementedError("Candidate generation will be implemented next.")
